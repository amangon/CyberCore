# Production environment variables — committed to source control.
# These are baked into the Next.js build at compile time.
# Override at runtime by setting environment variables in Render dashboard.
NEXT_PUBLIC_API_BASE_URL=https://cybercore-backend-csqd.onrender.com/api
